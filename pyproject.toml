[project]
name = "voiceforge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "beecall17", email = "bikalpoudel884@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=3.2.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
voiceforge = "voiceforge:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
